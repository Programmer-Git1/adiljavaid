#include<stdio.h>
int main()
{
  int number, sum =0, Average;
  for(int i =1; i <=5; i++)
  {
      scanf("%d",&number);
    sum = sum + number;
    Average = sum/i;
  }

   printf("The sum of five numbers is %d\n", sum);
   printf("The average of five number sum = %d", Average);
}
